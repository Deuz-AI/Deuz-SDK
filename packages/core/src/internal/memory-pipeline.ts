/** Narrow lazy entry: only the optional loop memory pipeline, not the full public namespace. */
export {
  recall,
  remember,
  sweepExpired,
  formatMemoriesForPrompt,
  defaultMemoryScorer,
} from '../memory';
