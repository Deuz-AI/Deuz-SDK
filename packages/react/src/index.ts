/**
 * @deuz-sdk/react — React bindings for the Deuz SDK.
 *
 * The 1.7.0 surface (useChat v2 with chatId/cost/budget/approval state,
 * useObject, headless ToolApprovalCard/CostBadge) lands here task by task.
 * Until then `@deuz-sdk/core/react` remains the shipping hook surface.
 */
export {};
